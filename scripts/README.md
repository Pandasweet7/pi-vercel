# Scripts

Placeholder for tooling added during implementation:

- `push-image.mjs` (M4) — build the pi-web sandbox image and push to Vercel Container Registry.
- `smoke.mjs` (M1) — local end-to-end check: auth → sandbox.getOrCreate → proxy → status.

Nothing here is required for the v1 prototype (managed image + install at onCreate).
